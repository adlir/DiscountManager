namespace Constants
{
    public static class DiscountConstants
    {
        public const int CutOffInYears = 5;
        public const decimal PercentageNewAccount = (decimal)5 / 100;
        public const decimal PercentagePerYear = (decimal)1 / 100;
    }
}